# Skill Bartering Platform (Backend)

## Setup
- Create `backend/.env`
- Install deps: `npm install`
- Run: `npm run dev`

## Scripts
- `npm run dev` - starts the API server

## Health check
- `GET /health`
